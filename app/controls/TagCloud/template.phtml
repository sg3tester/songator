<div class="hTagCloud">
	<ul class="{$sortClass}">
		{foreach $tags as $tag}
			<li class="{$tag->class}"><a href="{plink $destination, $tagKey => $tag->url}">{$tag->name}</a></li>
		{/foreach}
	</ul>
</div>